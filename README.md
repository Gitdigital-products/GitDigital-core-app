# GitDigital-core-app
gitdigital-core repository will set the foundation for everything else
# GitDigital Core App

This is the frontend & small API for GitDigital Core — built with Next.js + Tailwind.

## Quick start (dev)
1. `cd web`
2. `cp .env.example .env.local` and update
3. `npm install`
4. `npm run dev`

## Key features
- Next.js app (React)
- Tailwind CSS
- Minimal API routes for server-side actions
- Dockerfile + docker-compose for local dev